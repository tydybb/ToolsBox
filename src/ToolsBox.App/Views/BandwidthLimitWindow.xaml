<Window x:Class="ToolsBox.App.Views.BandwidthLimitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置上传限速" Width="430" Height="310" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#F4F6F8" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="设置上传速度上限" FontSize="21" FontWeight="SemiBold" Foreground="#1F2933"/>
            <TextBlock x:Name="ApplicationText" Foreground="#637381" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="当前版本只限制上传速度，不限制下载。规则按程序路径保存，软件重启后仍然生效。"
                   TextWrapping="Wrap" Foreground="#8A4B08" Background="#FFF7ED" Padding="10" Margin="0,16,0,12"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="128 KB/s" Tag="128|KB" Click="OnPreset" Margin="0"/>
            <Button Content="512 KB/s" Tag="512|KB" Click="OnPreset"/>
            <Button Content="1 MB/s" Tag="1|MB" Click="OnPreset"/>
            <Button Content="5 MB/s" Tag="5|MB" Click="OnPreset"/>
        </StackPanel>
        <Grid Grid.Row="3" Margin="0,18,0,0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
            <TextBox x:Name="ValueTextBox" Text="1" VerticalContentAlignment="Center"/>
            <ComboBox x:Name="UnitComboBox" Grid.Column="1" Margin="10,0,0,0" SelectedIndex="1">
                <ComboBoxItem Content="KB/s" Tag="KB"/>
                <ComboBoxItem Content="MB/s" Tag="MB"/>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Button Content="解除限速" HorizontalAlignment="Left" Click="OnRemove" Foreground="#9B2C2C" Background="#FFF2F2" BorderBrush="#EAA4A4" Margin="0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" IsCancel="True"/>
                <Button Content="应用" Click="OnApply" IsDefault="True" Background="#246BFD" Foreground="White" BorderBrush="#246BFD"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
